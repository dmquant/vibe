---
source: ai-institute
resource_type: research-result
report_kind: daily
analyst_level: "Unknown Analyst Level"
content_error: ""
---

# 研究院每日诊断报告 — 2026-05-05

Kind: **daily**
Analyst: **(unknown)**

## Links

- Report type: [[daily]]
- Analyst: [[Unknown Analyst]]
- Analyst level: [[Unknown Analyst Level]]

# 研究院每日诊断报告 — 2026-05-05

*诊断师生成时间: 2026-05-05 · 数据窗口: 2026-05-04T16:01:08Z（过去24小时）*

---

## 1. 一句话总览

研究院今日整体运行稳定，调度流水线零失败、零救援标记，但**积压待处理运行数（26个）超过窗口内已完成数（21个）**，同时 `chief-risk` 在白板与邮箱双渠道均出现显著超配，提示 QA 路由存在过度偏向审查员的风险，状态评级：**观察列表（Watch-listed）**。

---

## 2. 关键指标

| 指标 | 数值 | 备注 |
|---|---:|---|
| 托管工作流总数 | 215 | — |
| 窗口内运行数 | 21 | 全部 completed，完成率 100% |
| 待处理运行数（parked） | **26** | 超过已完成数，需关注 |
| 窗口内邮箱线程 | 49 | 全部为自动移交（auto-handoff） |
| 待处理临时派发 | 0 | 无积压 |
| 白板会话 | 23 | 共 165 张卡片 |
| 白板卡片（running） | 34 | 占全部卡片 20.6% |
| 白板卡片（failed） | 1 | — |
| 执行档案条目 | 0 | failed=0, salvage_markers=0 |
| 窗口内事件总数 | 181 | — |

---

## 3. 异常与模式

### 3.1 待处理运行积压（26 pending > 21 completed）
- **观察**：窗口内已完成运行 21 个，但"parked/awaiting terminal"的待处理运行达 **26** 个，二者比值 1.24×。
- **意义**：积压量超过当日产出，若这批 parked 任务已超过 2 小时未推进至终态，可能指向调度卡死或依赖链断裂。无法从快照直接判断积压时长，需运营员核查时间戳。
- **置信度**：中（数据可见，但无时间戳细节）

### 3.2 `chief-risk` 在两条渠道均过度接收路由
- **观察**：
  - 邮箱自动移交：`chief-risk` 接收 **4 次**，占总量 49 的 8.2%，与一线分析师 tmt-analyst（6）、chief-strategist（6）、factor-analyst（5）同量级。
  - 白板移交：`chief-risk` 接收 **7 次**，是白板中接收量最大的单一角色之一，与 energy-analyst（11）、global-macro（9）、industrials-analyst（9）、utilities-analyst（8）在同一数量级，远高于审查员正常比例。
- **意义**：`chief-risk` 是风控审查员角色，高频接收工作移交暗示 QA 步骤提示词存在过度路由至审查员的问题，可能导致合规/风控角色的工作负载过重，并拉长研究流水线的审批链路。
- **置信度**：高

### 3.3 白板卡片中 34 张仍处于 `running` 状态
- **观察**：165 张卡片中，completed=130，running=**34**（占 20.6%），failed=1。
- **意义**：快照生成时仍有 1/5 的卡片在运行中，若这批卡片在正常时间段内将完成则属正常；但若部分已运行超时，则为潜在卡死。Events 中 `whiteboard.card.completed=138`（高于 completed card count 130），可能存在重复事件或卡片多次完成的情况。
- **置信度**：中

### 3.4 `sentiment-analyst` 发件高度集中
- **观察**：邮箱自动移交发件人中，`sentiment-analyst` 贡献 **14/49**（28.6%），`convertible-analyst` 贡献 9/49（18.4%），前二者合计占 47%。
- **意义**：单一发件人占近三成，若 sentiment-analyst 的下游分析师（tmt-analyst、factor-analyst等）处理速度跟不上，将形成瓶颈。目前下游无明显积压（dispatch completed=48，running=1），但属于结构性集中度风险。
- **置信度**：中

### 3.5 白板移交中"无接收人"（none）占最大份额
- **观察**：白板移交中 `(none)` 共 **36 次**，是所有单一接收人中最大的分组（占总 165 张的 21.8%），超过第二名 energy-analyst（11）三倍以上。
- **意义**：部分卡片完成后未触发任何移交，若这些卡片是终态节点（无需再移交）则正常；但如果其中包含本应路由给一线分析师的研究产出，则为路由静默丢失。
- **置信度**：低（快照无法区分"设计上无需移交"与"路由失败"）

### 3.6 3 个运行记录 `workflow_kind=(none)`，1 个 `task_key=(none)`
- **观察**：task_key 分布中出现 `(none)` 1 次；workflow_kind 分布中 `(none)` 出现 3 次（routine=17, global=1）。
- **意义**：这些记录可能是测试运行、孤儿工作流或新增工作流尚未完成分类。`global` 类型仅 1 次，与 `(none)` 难以区分是否为同一批次遗留问题。
- **置信度**：低

### 3.7 Events 计数与 Card 状态存在轻微不符
- **观察**：`whiteboard.card.completed` 事件 = 138，但 card status completed = 130（差 8）；`whiteboard.card.failed` 事件 = 2，但 card status failed = 1（差 1）。
- **意义**：可能由重试/重入导致同一张卡片多次触发 completed 事件，或存在事件去重逻辑缺失。属于轻微数据一致性问题，不影响业务但值得追踪。
- **置信度**：中

---

## 4. 推荐优化

### 优化 1（优先级：高）— 收紧 QA 步骤的 `chief-risk` 路由条件

- **目标**：QA/移交步骤提示词，涉及 whiteboard 多个 session 的移交分配逻辑
- **建议修改**：在 QA 步骤的路由提示中，为 `chief-risk` 添加显式触发条件限定，例如：仅当内容涉及"杠杆超限、信用风险、监管合规、对手方暴露"等关键词时才路由至 `chief-risk`；其他市场分析内容默认路由至对应一线分析师（equity-strategy、macro、sector 等）。建议同时为 `chief-risk` 设置每日上限提示（如"本日 chief-risk 路由不超过 2 次"）作为过渡保护。
- **依据**：§3.2——白板 7 次 + 邮箱 4 次共 11 次路由至 `chief-risk`，在 49 次邮箱移交和 165 张白板卡片中占比异常偏高。
- **风险**：过度收紧可能导致真正需要风控复核的内容绕过 `chief-risk`，建议初期设置宽松阈值并监控两周。

---

### 优化 2（优先级：高）— 核查并清理 26 个 parked 运行积压

- **目标**：调度器 / 运行管理系统（pending runs queue）
- **建议操作**：运营员手动查询所有 parked 运行的创建时间戳，识别超过 2 小时仍未进入终态的运行。对于已超时的运行，检查其依赖的上游任务状态；若依赖链已断裂，手动推进至 failed 或取消，避免积压持续膨胀。若 26 个 parked 运行均属正常批次尾部（尚未到期），则此建议可推迟执行。
- **依据**：§3.1——parked 数（26）> completed 数（21），比值 1.24×，提示存在潜在调度滞后。
- **风险**：强制终止运行可能丢失正在进行中的有效分析；建议仅对明确超时（>2h）的运行采取干预，其余观察。

---

### 优化 3（优先级：中）— 归类或清理 `workflow_kind=(none)` 运行记录

- **目标**：工作流注册/元数据配置
- **建议修改**：为所有活跃工作流补充 `workflow_kind` 与 `task_key` 字段。对 task_key=(none) 的运行记录进行溯源，确认是测试运行还是配置遗漏；若为前者，建议在注册时强制校验这两个字段非空。
- **依据**：§3.6——3 个 kind=(none) 和 1 个 task_key=(none) 的运行记录是数据质量噪声，长期积累将影响运营分析准确性。
- **风险**：低——纯元数据修复，不影响业务逻辑。

---

## 5. 给操作员的开放问题

1. **26 个 parked 运行的时龄是多少？** 请查询这批运行的 `created_at` 时间戳，确认是否有任何已超过 2 小时仍未进入终态。这是判断是否存在调度卡死的关键信息。

2. **`chief-risk` 的高频路由是否有业务背景？** 近期是否有特殊合规事件、监管窗口、或市场异动（如信用事件）导致风控角色需要审查更多内容？如果有，当前路由量可能是合理的，优化 1 的干预应推迟。

3. **白板中 36 张"无移交接收人"的卡片是终态设计还是路由遗漏？** 请确认这些 `(none)` 卡片在工作流中是否被设计为自终止节点（产出已上传、无需转发），还是本应路由给下游分析师但因逻辑缺失而静默丢弃。

---

*本报告为纯顾问性质，不直接修改任何提示词、工作流配置或系统状态。所有数字取自快照原始数据，无模型层面的推断。*
