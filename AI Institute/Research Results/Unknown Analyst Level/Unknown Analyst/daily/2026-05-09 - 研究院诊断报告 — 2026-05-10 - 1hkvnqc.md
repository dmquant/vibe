---
source: ai-institute
resource_type: research-result
report_kind: daily
analyst_level: "Unknown Analyst Level"
content_error: ""
---

# 研究院诊断报告 — 2026-05-10

Kind: **daily**
Analyst: **(unknown)**

## Links

- Report type: [[daily]]
- Analyst: [[Unknown Analyst]]
- Analyst level: [[Unknown Analyst Level]]

# 研究院诊断报告 — 2026-05-10

数据锚点：本报告按 shell 日期 `2026-05-10` 解释“今日 / 昨日 / 本周 / 上周 / 上一交易日”等相对时间；输入快照为 `Fleet Health Snapshot — 2026-05-10`，生成时间 `2026-05-09T16:00:18.381Z`（来源：`fleet_health.md:1-3`）。

## 1. 一句话总览

今日研究院总体 **watch-listed**：核心执行链路完成率健康、无执行归档失败或 salvage markers，但 `25` 个 parked pending workflow runs 与白板/事件计数不一致提示终态收敛和监控一致性需要关注（来源：`fleet_health.md:9-16`, `fleet_health.md:91-127`）。

## 2. 关键指标

| 指标 | 数值 | 诊断含义 | 来源 |
|---|---:|---|---|
| Managed workflows | 224 | 管理面规模稳定的基准分母 | `fleet_health.md:9` |
| Runs in window | 6 | 今日窗口内 workflow runs 数量较低，且全部 completed | `fleet_health.md:10`, `fleet_health.md:24-43` |
| completed workflow runs | 6 | 窗口内 workflow run 终态成功率为 100% | `fleet_health.md:24-26` |
| Pending runs parked awaiting terminal | 25 | parked pending 量高于今日完成 run 数，需要关注终态落账 | `fleet_health.md:11` |
| Mailbox threads / auto-handoffs | 17 / 17 | 今日 mailbox 全部来自 auto-handoff | `fleet_health.md:12`, `fleet_health.md:49-61` |
| Mailbox running | 1 thread / 1 dispatch | auto-handoff 仍有 1 条未终态 | `fleet_health.md:51-61` |
| Pending adhoc dispatches | 0 | 临时派发没有积压 | `fleet_health.md:13` |
| Whiteboard sessions / cards | 20 / 280 | 白板协作量显著高于 workflow run 量 | `fleet_health.md:14` |
| Whiteboard card status | 279 completed / 1 running | 白板卡片总体完成率高，但仍有 1 张 running | `fleet_health.md:91-96` |
| Failure counts | execution-archive failed 0；whiteboard.card.failed 1 | 执行归档失败为 0，但事件层出现 1 次白板卡片失败 | `fleet_health.md:16`, `fleet_health.md:120-127` |
| Salvage markers | 0 | 没有发现声明输出文件频繁丢失的信号 | `fleet_health.md:16` |

## 3. 异常与模式

- **Parked workflow runs 偏高**：今日窗口内只有 `6` 个 workflow runs 且全部 completed，但 pending runs parked awaiting terminal 为 `25`，是今日 completed run 数的 `4.17x`（来源：`fleet_health.md:10-11`, `fleet_health.md:24-26`）。这可能意味着历史或跨窗口 run 的终态回写、清理或可观测性存在滞后；快照未给出 pending run 年龄或 IDs，因此无法确认是否存在超过 2h 的老 pending。置信度：medium。

- **执行入口量低而协作层量高**：workflow runs in window 仅 `6`，task_key 全部为 `institute_diagnosis` 与 weekend topic pitch 系列，各 `1` 次，workflow_kind 全部为 routine；同时白板 sessions 为 `20`、cards 为 `280`，事件为 `324`（来源：`fleet_health.md:10`, `fleet_health.md:28-43`, `fleet_health.md:14-15`）。这说明今日主要负载在协作/白板层而非 workflow run 层；若这是周末预期则正常，若不是，可能存在调度覆盖不足或 run 记录口径偏窄。置信度：medium。

- **Auto-handoff 主题流向集中到 TMT**：auto-handoff 总量 `17`，其中 `tmt-analyst` 接收 `6` 条，占 `35.3%`；发送方 `thematic-researcher`、`sentiment-analyst`、`social-media-analyst` 各 `4` 条，合计 `12/17 = 70.6%`（来源：`fleet_health.md:63-85`）。这不是 reviewer 过度路由：`chief-risk` 仅 `1` 条，未出现 `qa-manager` 或 `compliance-officer` 的 mailbox 接收集中；但它提示 weekend pitch 流程可能把情绪/社媒/主题信号过多汇入 TMT，而不是更均衡地分配给宏观、策略、行业或资产配置主责人。置信度：high。

- **Whiteboard handoff 偏向 overlay/组合角色，且有未指定接收人**：白板接收分布中 `asset-allocator` 为 `29`、`chief-strategist` 为 `28`，两者合计 `57/280 = 20.4%`；`(none)` 为 `21`，占 `7.5%`（来源：`fleet_health.md:98-116`）。主分析师仍有参与，例如 `tmt-analyst` `20`、`utilities-analyst` `18`、`energy-analyst` `18`、`industrials-analyst` `17`，但白板协作首层明显偏向组合/总策略角色；若卡片本应先由一级分析师归因，可能增加总策略或资产配置端的整理负担。置信度：medium。

- **Reviewer 路由没有明显失控，但白板 QA 仍需抽样复核**：白板中 `qa-manager` `12`、`chief-risk` `9`，合计 `21/280 = 7.5%`；mailbox 中 reviewer 类仅 `chief-risk` `1/17 = 5.9%`（来源：`fleet_health.md:73-85`, `fleet_health.md:98-116`）。这不像 QA-step prompt 大规模 over-routing reviewers；更可能是正常审阅或风险抽样。置信度：medium。

- **白板卡片状态与事件计数存在轻微不一致**：白板 card status 显示 `279 completed`、`1 running`，但事件层显示 `whiteboard.card.completed` 为 `280`，并且有 `whiteboard.card.failed` `1`（来源：`fleet_health.md:91-96`, `fleet_health.md:120-127`）。这可能来自事件去重口径、重试后成功、或状态快照与事件流采样窗口不完全一致；虽然规模很小，但会影响日后 failure-rate 和 SLA 判断。置信度：medium。

- **Salvage marker 风险低**：execution-archive entries 为 `0`，failed 为 `0`，salvage markers 为 `0`（来源：`fleet_health.md:16`）。今日没有模型频繁丢失声明输出文件的证据，因此不建议把 prompt 强化重点放在 artifact salvage 上。置信度：high。

## 4. 推荐优化

1. **Target: `<system component:workflow-terminal-reconciler>`**  
   **Suggested change:** 增加一个只读终态对账守护逻辑或日报字段：把 `Pending runs (parked, awaiting terminal)` 按年龄分桶输出为 `<2h`、`2-6h`、`6-24h`、`>24h`，并列出最老 10 个 run 的 `workflow_key`、`step_id`、`run_id`、`last_event_at`。若已有内部对账器，则把该字段加入 fleet-health snapshot，不直接自动关闭 run。  
   **Rationale:** 今日 `25` 个 parked pending runs 高于 `6` 个 completed workflow runs，且快照没有年龄/ID，无法判断是否存在真正卡死的老 pending（来源：`fleet_health.md:10-11`）。  
   **Risk:** 如果采集口径把正常跨日 parked run 误标为风险，会增加操作员噪声；因此首版应只读展示，不自动修复。

2. **Target: `<topic_pitch_weekend_*:auto_handoff_step>`**  
   **Suggested change:** 在 auto-handoff prompt 中加入接收人选择约束：“若主题证据主要来自 sentiment/social/thematic 信号，先判定 primary owner；只有当 catalyst 明确属于 TMT 公司、平台、AI、半导体、消费电子或软件链条时才默认发送 `tmt-analyst`。否则优先路由至对应宏观、策略、行业或资产配置主责人，并在 handoff rationale 中写出 owner 判定依据。”  
   **Rationale:** 今日 auto-handoff `17` 条中 `tmt-analyst` 接收 `6` 条（`35.3%`），且三类上游主题/情绪/社媒发送方合计 `12/17 = 70.6%`，提示周末 pitch 流可能偏向把多源信号汇入 TMT（来源：`fleet_health.md:63-85`）。  
   **Risk:** 约束过强可能把真实 TMT 交叉主题分散到不熟悉技术链条的 owner，降低召回率；需要保留“明确 TMT catalyst”例外。

3. **Target: `<system component:whiteboard-event-normalizer>`**  
   **Suggested change:** 为 whiteboard card 统计增加 reconciliation note：当 `card status` 与 `event_type` 计数不一致时，输出差异解释字段，例如 `retry_success_count`、`duplicate_completed_events`、`failed_then_completed_count` 或 `window_boundary_mismatch_count`。  
   **Rationale:** 今日 card status 为 `279 completed / 1 running`，但事件层为 `whiteboard.card.completed 280` 与 `whiteboard.card.failed 1`，说明状态快照和事件流至少有 `1` 个单位的口径差异（来源：`fleet_health.md:91-127`）。  
   **Risk:** 事件归因字段如果实现不严谨，可能制造新的伪精确性；应先在快照中标注“口径解释”，再用于 SLA 或 failure-rate 自动告警。

## 5. 给操作员的开放问题

1. `25` 个 parked pending workflow runs 中，有多少是预期跨日 parked，多少应在 `2026-05-10` 前完成终态落账？
2. weekend topic pitch 是否业务上预期更偏 TMT？如果不是，`tmt-analyst` `6/17` 的 auto-handoff 占比是否应设置软阈值告警？
3. 白板 `(none)` 接收人 `21/280` 是否代表无需 owner 的系统卡片，还是缺少 handoff recipient 的可修复数据质量问题？
