---
source: ai-institute
resource_type: research-result
report_kind: daily
analyst_level: "Unknown Analyst Level"
content_error: ""
---

# 研究院诊断报告 — 2026-05-13

Kind: **daily**
Analyst: **(unknown)**

## Links

- Report type: [[daily]]
- Analyst: [[Unknown Analyst]]
- Analyst level: [[Unknown Analyst Level]]

# 研究院诊断报告 — 2026-05-13

*快照时间: 2026-05-12T16:00:13.629Z · 范围: 过去 24 小时*

---

## 1. 一句话总览

研究院今日整体**健康**:26 个工作流全部 `completed`、无失败、无 salvage 标记;但白板路由偏向风控/策略端(`chief-risk`、`chief-strategist` 等),且存在 4 个 parked 待终结的运行,需要观察。

---

## 2. 关键指标

| 指标 | 数值 |
|---|---:|
| Managed workflows | 224 |
| Runs in window | 26 (completed=26, failed=0) |
| Pending runs (parked) | **4** |
| Mailbox threads (auto-handoffs) | 25 (completed=24, running=1) |
| Pending adhoc dispatches | 0 |
| Whiteboard cards | 72 (completed=70, running=2) |
| Whiteboard sessions | 9 |
| Events | 106 |
| Execution-archive (failed / salvage) | 0 / 0 |

---

## 3. 异常与模式

1. **Auto-handoff 接收方高度集中于策略端**(置信度:**高**)
   - 证据:25 次 auto-handoff 中,`ashare-strategist` 7 次 + `chief-strategist` 7 次 = **56%** 流向两名策略师;`tmt-analyst` 与 `global-macro` 各 4 次,合计 88% 集中在 4 个角色。
   - 影响:策略端可能成为瓶颈;一线分析师(macro/sector/equity-strategy 等)未被作为下游主受体,说明 QA-step 或路由规则可能在向"上层综合者"过度集中。

2. **白板交接出现 `(none)` 占首位**(置信度:**高**)
   - 证据:Whiteboard handoff recipient 分布中 `(none)` 计 8 张卡片,排第一,占 72 张完成卡的约 11%。
   - 影响:这些卡片完成后没有声明下游接收方,可能意味着卡片产出后无人接续,信息流断在白板层。

3. **白板交接同时覆盖 `chief-risk` 4 次**(置信度:**中**)
   - 证据:白板接收方分布出现 `chief-risk` (4) 与 `chief-strategist` (4),与 mailbox 中策略端集中现象互为佐证。
   - 影响:reviewer 类角色(risk/strategist)正同时承担白板与 mailbox 双通道压力;若 QA-step 模板默认 cc 这些角色,会放大集中。

4. **4 个 parked pending runs 未在窗口内收敛**(置信度:**中**)
   - 证据:`Pending runs (parked, awaiting terminal): 4`,但执行归档无失败、无 salvage 标记。
   - 影响:这 4 个 run 处于"已完成核心步骤但未走完终结链路"状态,可能是 mailbox/whiteboard 上 1 个 running 线程 + 2 张 running 白板卡片所牵制——需要在下一窗口验证是否自然收敛。

5. **task_key 分布高度同质化、集中在 `topic_pitch_*`**(置信度:**中**)
   - 证据:26 次 run 中至少 14 次为 `topic_pitch_*` 系列(overnight/morning/premarket/midday/intraday/close/postclose),每个 task_key 都恰好 1 次。
   - 影响:研究院当日产出几乎全部为"选题/快讯"型,缺少深度分析类 task(如行业、估值、组合)。属于产能结构信号,而非故障。

6. **salvage_marker_window = 0、failed = 0**(置信度:**高**)
   - 证据:执行归档 0/0。
   - 影响:正面信号——本窗口模型未出现输出文件丢失或失败,prompt 层目前稳定。

---

## 4. 推荐优化(按预期影响排序)

### 4.1 收紧 QA-step / mailbox 路由模板,减少向策略端过度集中
- **目标**:`<system component: auto-handoff QA-step prompt>` 或 mailbox 路由规则。
- **建议变更**:在 QA-step 模板中增加约束——"当主题为单一行业 / 单一板块时,默认 handoff 给对应的一线分析师(sector/industry analyst),只有跨板块综合或风险议题才升级至 `chief-strategist` / `ashare-strategist` / `chief-risk`"。同时把 `chief-strategist + ashare-strategist` 合计的 daily handoff 占比硬约束在 ≤ 35%。
- **依据**:见 §3.1 与 §3.3,两名策略师承接 56% 流量。
- **风险**:若分析师覆盖不全或 routing 关键词分类错误,可能出现一线分析师被误派、综合议题被漏接的情况;需要保留"无法分类则回退到 strategist"兜底。

### 4.2 白板卡片必须声明下游接收方(消灭 `(none)`)
- **目标**:`<system component: whiteboard card schema / closing-step prompt>`。
- **建议变更**:在白板卡片的 closing-step 中,把 `handoff_recipient` 字段从可选改为**必填**;允许的特殊值仅 `archive`(明确无下游)或一个具名角色。模型若未声明,closing-step 必须 fail-loud 而非静默通过。
- **依据**:§3.2,`(none)` 8/72 = 11% 静默断流。
- **风险**:短期内可能出现卡片 closing 失败率上升、需要人工补 `archive` 标签;建议先以告警(soft warn)上线一窗,再切硬约束。

### 4.3 给 parked pending runs 加上"超过 N 小时未终结"软告警
- **目标**:`<system component: pending-run watcher>`。
- **建议变更**:对状态为 parked 且 `created_at` > 2h 的 run,在下一份 fleet-health 中独立列出 ID 与卡住的子组件(running mailbox thread / running whiteboard card),方便诊断师定位。
- **依据**:§3.4,4 个 parked run + 1 running thread + 2 running cards,但当前快照不暴露 ID,无法追踪。
- **风险**:噪声风险低;但需要 worker 端增字段,改动不在 prompt 层。

---

## 5. 给操作员的开放问题

1. 4 个 parked pending runs 的 ID 是哪些?是否对应仍在 running 的 1 条 mailbox thread + 2 张白板卡片?能否在下一份 fleet-health 中暴露这些 ID。
2. 当前 QA-step / auto-handoff 模板对"何时升级到 chief-strategist / ashare-strategist"是否有明文规则?若有,是否近期被改动,导致集中度上升?
3. 今日 task 几乎全为 `topic_pitch_*`,是否反映调度计划本身的安排,还是深度类 task(行业研究、组合再平衡)被静默跳过?
